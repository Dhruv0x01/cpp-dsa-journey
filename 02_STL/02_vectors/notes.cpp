#include <bits/stdc++.h>
using namespace std;

// Understand every possible functions of vector because these functions will 
// be similar in all the other containers like queue, list, map, set 
void explainVector(){
    
}

int main(){
    explainVector();
    return 0;
}